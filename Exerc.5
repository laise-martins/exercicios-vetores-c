#include <stdio.h>

int main() {
    int vetor[10];
    int i;

    for(i = 0; i < 10; i++) {
        printf("Digite um numero: ");
        scanf("%d", &vetor[i]);
    }

    printf("\nVetor invertido:\n");

    for(i = 9; i >= 0; i--) {
        printf("%d ", vetor[i]);
    }

    return 0;
}
