#include <stdio.h>

int main() {
    int vetor[10];
    int i, soma = 0;

    for(i = 0; i < 10; i++) {
        printf("Digite um numero: ");
        scanf("%d", &vetor[i]);
        soma += vetor[i];
    }
    printf("\nSoma = %d\n", soma);

    return 0;
}
